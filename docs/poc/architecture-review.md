# アーキテクチャレビュー結果

## 概要

2024年12月実施のアーキテクチャレビュー結果をまとめる。
バックエンド（クリーンアーキテクチャ/DDD）とフロントエンド（Next.js App Router/BFF）の設計・実装を評価。

---

## 総合評価

| 観点 | 評価 | コメント |
|------|------|---------|
| レイヤー分離 | ⭐⭐⭐⭐ | クリーンアーキテクチャに準拠 |
| DDDパターン | ⭐⭐⭐⭐ | Entity/VO/Service の使い分け適切 |
| 型安全性 | ⭐⭐⭐ | 一部で型キャストの回避策あり |
| エラーハンドリング | ⭐⭐ | 改善の余地あり |
| テスタビリティ | ⭐⭐⭐ | DIは実装済み、一部改善必要 |

---

## 強み

### バックエンド

| 項目 | 説明 |
|------|------|
| ドメインモデルの充実 | `CANSLIMScore`, `PerformanceMetrics` など豊富な値オブジェクト |
| Entity の不変性 | `frozen=True` で意図しない変更を防止 |
| リポジトリ抽象化 | Interface → Implementation で差し替え可能 |
| ユースケース分離 | screener/portfolio/market で明確に分離 |
| ドメインサービス | 複雑な計算ロジック（RS Rating、パフォーマンス計算等）を適切に分離 |

### フロントエンド

| 項目 | 説明 |
|------|------|
| BFFパターン | バックエンドURLを隠蔽、CORS不要 |
| Hooks = Application Layer | 状態管理とユースケースを集約 |
| 型定義の充実 | `types/` でドメインモデルを反映 |
| コンポーネント分離 | 機能別にディレクトリ分割 |

---

## 問題点一覧

### 優先度定義

| 優先度 | 説明 |
|--------|------|
| P0 | 重大：機能不全またはデータ整合性に影響 |
| P1 | 高：保守性・テスタビリティに影響 |
| P2 | 中：設計上の不整合 |
| P3 | 低：コード品質・可読性 |

---

### P0: 重大

#### ISSUE-001: Async/Sync ミスマッチ

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/infrastructure/repositories/postgres_trade_repository.py` |
| 問題 | `async` と宣言しているが、SQLAlchemy の同期セッションを使用。`await` がない |
| 影響 | 非同期処理が実際には機能していない。将来的なパフォーマンス問題の原因 |
| 対策 | (A) `AsyncSession` + `await` に変更、または (B) `async` を削除して同期処理に統一 |
| 状態 | 🔴 未対応 |

```python
# 現状（問題）
async def get_by_id(self, trade_id: int) -> PaperTrade | None:
    result = self.session.execute(...)  # await がない

# 対策A: 非同期化
async def get_by_id(self, trade_id: int) -> PaperTrade | None:
    result = await self.session.execute(...)

# 対策B: 同期化
def get_by_id(self, trade_id: int) -> PaperTrade | None:
    result = self.session.execute(...)
```

---

#### ISSUE-002: 例外の握りつぶし

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/application/use_cases/screener/screen_canslim_stocks.py` |
| 問題 | `except Exception: continue` で全例外を無視 |
| 影響 | サイレント障害、デバッグ困難 |
| 対策 | ロギング追加、特定例外のハンドリング |
| 状態 | 🔴 未対応 |

```python
# 現状（問題）
try:
    # 処理
except Exception:
    continue  # エラーがログにも残らない

# 対策
try:
    # 処理
except SpecificException as e:
    logger.warning(f"Failed to process: {e}")
    continue
except Exception as e:
    logger.error(f"Unexpected error: {e}")
    raise
```

---

### P1: 高

#### ISSUE-003: 型キャストによる回避策

| 項目 | 内容 |
|------|------|
| 場所 | `frontend/src/lib/api.ts` |
| 問題 | `as unknown as` による型安全性の放棄 |
| 影響 | 実行時エラーの可能性、TypeScriptの利点が失われる |
| 対策 | 適切なBFFルートを実装するか、型定義を修正 |
| 状態 | 🔴 未対応 |

```typescript
// 現状（問題）
export async function getDetailedPerformance(...): Promise<ApiResponse<DetailedPerformanceResponse>> {
  return getPerformance(...) as unknown as Promise<ApiResponse<DetailedPerformanceResponse>>;
}

// 対策: 専用ルートを実装
// frontend/src/app/api/performance/detailed/route.ts を作成
```

**対象箇所:**
- `getDetailedPerformance()` - 詳細パフォーマンスAPI
- `getOpenPositions()` - オープンポジションAPI

---

#### ISSUE-004: DI のハードコード

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/presentation/dependencies.py` |
| 問題 | Gateway/Calculator のインスタンス化がハードコード |
| 影響 | テスト時にモック困難、外部サービス依存 |
| 対策 | 設定ファイルまたはDIコンテナで注入可能に |
| 状態 | 🔴 未対応 |

```python
# 現状（問題）
def get_screen_canslim_use_case(...):
    gateway = YFinanceGateway()  # ハードコード
    return ScreenCANSLIMStocksUseCase(repo, gateway)

# 対策: ファクトリーパターン or 設定ベース
def get_financial_gateway() -> FinancialDataGateway:
    if settings.USE_MOCK_GATEWAY:
        return MockFinancialGateway()
    return YFinanceGateway()
```

---

#### ISSUE-005: インフラ層のエラーハンドリング不足

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/infrastructure/repositories/*.py` |
| 問題 | DB操作に対する例外処理がない |
| 影響 | 制約違反やコネクションエラーが未処理のまま伝播 |
| 対策 | カスタム例外タイプの定義とハンドリング |
| 状態 | 🔴 未対応 |

```python
# 対策例
class RepositoryError(Exception):
    pass

class EntityNotFoundError(RepositoryError):
    pass

class DuplicateEntityError(RepositoryError):
    pass
```

---

### P2: 中

#### ISSUE-006: エンドポイントパスの不整合

| 項目 | 内容 |
|------|------|
| 場所 | フロントエンド ↔ BFF ↔ バックエンド |
| 問題 | パス命名が層によって異なる |
| 影響 | メンテナンス時の混乱 |
| 対策 | パス命名を統一 |
| 状態 | 🟡 未対応 |

| クライアント (api.ts) | BFF Route | バックエンド |
|----------------------|-----------|-------------|
| `/watchlist` | `/api/watchlist` | `/portfolio/watchlist` |
| `/trades` | `/api/trades` | `/portfolio/trades` |
| `/performance` | `/api/performance` | `/portfolio/performance` |

---

#### ISSUE-007: DB タイムゾーン未考慮

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/infrastructure/database/models/*.py` |
| 問題 | `DateTime` カラムに `timezone=True` がない |
| 影響 | タイムゾーン関連のバグの可能性 |
| 対策 | `DateTime(timezone=True)` を使用 |
| 状態 | 🟡 未対応 |

```python
# 現状
created_at = Column(DateTime, default=datetime.now(timezone.utc))

# 対策
created_at = Column(DateTime(timezone=True), default=datetime.now(timezone.utc))
```

---

#### ISSUE-008: キャッシュ戦略の不統一

| 項目 | 内容 |
|------|------|
| 場所 | `frontend/src/app/api/*/route.ts` |
| 問題 | Route Handler ごとにキャッシュ設定がバラバラ |
| 影響 | 予期しないキャッシュ動作 |
| 対策 | キャッシュ戦略を明文化し統一 |
| 状態 | 🟡 未対応 |

| エンドポイント | 現状 | 推奨 |
|--------------|------|------|
| `/api/market/status` | `revalidate: 60` | 60秒 ✅ |
| `/api/market/indicators` | `revalidate: 30` | 30秒 ✅ |
| `/api/screener/canslim` | `revalidate: 0` | 0（リアルタイム）✅ |
| `/api/watchlist` | 未指定 | 0（ユーザー固有）|
| `/api/trades` | 未指定 | 0（ユーザー固有）|
| `/api/data/financials/*` | `revalidate: 300` | 5分 ✅ |

---

### P3: 低

#### ISSUE-009: 命名規則の不統一

| 項目 | 内容 |
|------|------|
| 場所 | `backend/src/application/use_cases/` |
| 問題 | Use Case の命名パターンが混在 |
| 影響 | 可読性・一貫性の低下 |
| 対策 | `{Verb}{Domain}UseCase` に統一 |
| 状態 | 🟢 未対応 |

| 現状 | 推奨 |
|------|------|
| `ScreenCANSLIMStocksUseCase` | `ScreenStocksUseCase` |
| `GetMarketStatusUseCase` | `GetMarketStatusUseCase` ✅ |
| `RecordTradeUseCase` | `ManageTradeUseCase` |

---

#### ISSUE-010: Portfolio ページにヘッダーなし

| 項目 | 内容 |
|------|------|
| 場所 | `frontend/src/app/portfolio/page.tsx` |
| 問題 | 他のページと異なり Header コンポーネントがない |
| 影響 | ナビゲーション体験の不整合 |
| 対策 | Header コンポーネントを追加 |
| 状態 | 🟢 未対応 |

---

#### ISSUE-011: lib/api.ts の冗長性

| 項目 | 内容 |
|------|------|
| 場所 | `frontend/src/lib/api.ts` |
| 問題 | BFFパターン導入により中間層として冗長 |
| 影響 | 不要なコード層 |
| 対策 | Hooks から直接 fetch を呼び出し、api.ts を削除 |
| 状態 | ✅ 対応完了（Phase 6） |

参照: [BFFリファクタリング計画](./refactoring/frontend-api-bff.md)

---

## 改善優先度マトリクス

```
緊急度
  高 │
     │  ┌─────────────────────────┐
     │  │ ISSUE-001: Async修正   │
     │  │ ISSUE-002: 例外処理    │
     │  └─────────────────────────┘
     │
     │          ┌─────────────────────────┐
     │          │ ISSUE-003: 型キャスト   │
     │          │ ISSUE-004: DI改善       │
     │          │ ISSUE-005: エラー処理   │
     │          └─────────────────────────┘
     │
     │                  ┌─────────────────────────┐
     │                  │ ISSUE-006〜008          │
     │                  └─────────────────────────┘
     │
     │                          ┌─────────────────────────┐
     │                          │ ISSUE-009〜011          │
     │                          └─────────────────────────┘
  低 └──────────────────────────────────────────────────────▶ 影響度 高
```

---

## 推奨対応順序

| 順序 | Issue | 工数 | 理由 |
|------|-------|------|------|
| 1 | ISSUE-002 | 小 | サイレント障害の防止 |
| 2 | ISSUE-001 | 中 | 非同期処理の正常化 |
| 3 | ISSUE-005 | 中 | エラーハンドリング基盤 |
| 4 | ISSUE-003 | 小 | 型安全性の回復 |
| 5 | ISSUE-004 | 中 | テスタビリティ向上 |
| 6 | ISSUE-007 | 小 | データ整合性 |
| 7 | ISSUE-006 | 小 | 保守性向上 |
| 8 | ISSUE-008 | 小 | パフォーマンス最適化 |
| 9 | ISSUE-009〜011 | 小 | コード品質向上 |

---

## 関連ドキュメント

- [サービスアーキテクチャ設計](./service-architecture.md)
- [BFFリファクタリング計画](./refactoring/frontend-api-bff.md)
- [API設計](./architectures/api-design.md)
- [ディレクトリ構造](./architectures/directory-structure.md)
